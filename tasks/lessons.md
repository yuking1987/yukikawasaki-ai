# lessons（自分向けルール／同じミスを繰り返さないため）

## 2026-07-16 案件コンテキスト蒸留（並列サブエージェント運用）
- **問題**: 複数ファイル担当のサブエージェントに任せたところ、1担当が「さらに孫担当を作り、孫は蒸留“本文テキスト”を返すだけでファイルに書かない」状態になり、context.md が作られなかった。
  - **なぜ**: 指示に「ファイルを書く」主体が曖昧だと、担当が“調整役”に回って作業を再委譲してしまう。孫は「本文を返す＝完了」と解釈する。
  - **どう直すか（次回ルール）**:
    1. 書き込みを伴う担当への指示は必ず「**あなた自身が Write/Edit でファイルを書く。サブエージェントを新たに作らない**」と明記する。
    2. 各担当の完了後は、**メイン側でファイルの実在をfilesystemで検証**する（`ls`/`grep -l`）。返事の文面だけで完了扱いしない。
- **書式**: サブエージェントの返答本文はしばしば `>` が `&gt;` にHTMLエスケープされる。ファイル化する際は `>` に直す（grep で `&gt;` 混入を最終チェック）。
- **機微情報**: 素材メールに本番FTP等の認証情報が平文で混じることがある。context.md には**値を転記せず「認証情報あり・取扱注意」の事実のみ**記す。最後に `ftp://user:pass@` 形式の漏洩を grep で確認する。
- **クライアント判定**: メール素材にはノイズが多い（相互リンク＝SEOバックリンク営業[backlink@]、ツール自動通知、営業メルマガ[配信停止リンク]、決済/SaaSサポート、協力/制作会社）。件数が多くても「制作/保守の実体があるか」で判定し、非該当は skip。
- **同一クライアントの別ソース**: フリーメール(gmail/ocn等)経由の素材が既存クライアントと同一のことがある（例: herb.ocn.ne.jp＝梅樹園、luck.ocn＝佐賀造船鉄工所）。**別ドメインでも署名の会社名で同一社か判定し、その会社の既存フォルダに集約**する（別フォルダを乱立させない）。ただし herb.ocn(梅樹園) と luck.ocn(佐賀造船) は**互いに別会社**なので混ぜない。

## 2026-07-16 オフィス表示：Codex専務の役割定義ミス（川崎さんフィードバック）
- **問題**: OfficeView の「Codex専務」バッジを、`status==="revision"`（要再考）の件数で「レビュー中 N」と表示していた。専務は**要再考の担当ではない**。
  - **正しい役割**: Codex専務＝**メンター役**。各社員が出した成果物のうち**重要なもの(importance:high)**を、Codexにレビュー・助言してもらう存在。要再考(revision)とは無関係。
  - **どう直したか**: バッジの母数を `importance==="high" && status==="pending" && review_status!=="passed"` に変更（[web/App.tsx] OfficeView の `codexReview`）。tooltipも「各社員の重要案件をレビュー・助言するメンター役」に修正。
  - **次回ルール**: キャラ/役割に紐づくUI指標を作るときは、**そのキャラの役割定義を先に確認**してから母数フィールドを選ぶ。`reviewed_by`/`review_status`/`importance` と `status(revision等)` は意味が別物。混同しない。
  - **未整備メモ**: `reviewed_by=codex` を自動で立てる処理は現状なし（GUI/API経由で記録する設計）。将来、高リスク案件を自動でCodexレビューに回すなら別途パイプライン実装が要る。

## 2026-07-16 追記：機微情報の扱い＆確認姿勢（川崎さんフィードバック）
- **機微情報は context.md に一切残さない（全般）**。パスワード値・Basic認証・スプレッドシート/DriveのURLとID・GitHubコミットハッシュ・請求書番号(INV…)・GA/UAプロパティID・**IPアドレス**・「パスワードの作り方（例：当日の年月日8桁）」まで、**ランダムな英数字の羅列と機微な仕組みはすべて削除**し「（値は別管理）」等に置換する。**既存の実例本文（メール全文）も対象**。
  - **なぜ**: 学習・流出リスク。平文の認証情報は絶対に含めない、が川崎さんの明確な要求。
  - **手順**: 蒸留/編集後、必ず grep で IP・google URL・INV・commit hash・パスワード値をスイープして残存ゼロを確認する。
- **判断に迷うことはすべて聞く（全般）**。特に「既存の上書き/統合可否」「クライアントか否かの境界」「機微情報の削除範囲」は独断で進めず AskUserQuestion で確認する。
- **報告文の書き方**: 複数の別会社を1行に並べて書くと「同一視した」と誤解される（例: 「herb=梅樹園、luck=佐賀造船 を統合」→川崎さんが“2社を混ぜた”と誤解）。**別会社は行を分けて明示**する。
- **照合インデックス**: `vault/70_references/clients-index.md`（ドメイン↔フォルダ↔正式名称↔宛名注意）を維持し、打ち返し前に照合する。名称ズレ（kkb=九州急行バス, sanrizkj=三立鑑定 等）はここで防ぐ。

## 2026-07-16 Slack取り込み＆蒸留（実行して分かったこと）
- **標準コネクタ `@modelcontextprotocol/server-slack` の制約**:
  - `slack_list_channels` は**公開チャンネルしか一覧化しない**。クライアント系は非公開が多く一覧に出ない。
  - **DMは基本取れない**（botが当事者でないDMはSlackの仕様で読めない）。DMは対象外と割り切る。
  - **非公開チャンネルもID直指定なら `slack_get_channel_history(channel_id)` で読める**（botが招待済みなら）。→ 次回は「読ませたいチャンネルのIDを人からもらう」前提で進める（チャンネル名クリック→最下部のチャンネルID）。
  - 招待直後、`slack_list_channels` の `is_member` は古い値（false）を返すことがある。membershipの真偽は**履歴が実際に読めるか**で判断する。
  - `slack_get_channel_history` は**ページング不可**（cursorパラメータなし）。limit=200等で最新分のみ取得。
- **本人特定**: Slack user ID は `slack_get_users` の profile.email で照合（川崎さん = `U0E2PHJJD`）。抽出は `user==本人ID && subtypeなし` で行い、bot/join通知/引用を除く。
- **⚠️ 機微情報ルールを反映指示に必ず入れる**: 今回、書き込み担当への指示に「機微情報は残さない」を書き忘れ、**IPアドレス(153.126.136.100)とzip解凍PWの実値(spc_2_jml)がcontext.mdに混入**した。事後の grep スイープ（IP/パスワード値/google URL/INV/commit hash）で回収・「（値は別管理）」に置換して復旧。
  - **次回ルール**: ファイル書き込みを伴うサブエージェントへの指示テンプレに、必ず [[lessons.md の機微情報ルール]]（IP・パスワード値・URL/ID・commit hash・INVは転記せず「値は別管理・取扱注意」の事実のみ）を明記する。反映後は毎回スイープを実行する。
  - 「パスワードを平文送付している」等の**注意書き（事実の記述）は残してよい**。削除するのは"値そのもの"だけ。
- 「毛利様チャンネル」と思ったIDが実は電話代行(fondesk)の入電ログbotチャンネルだった。**IDの中身は取得して実物で確認**してから用途を決める（名前や思い込みで役割を決めない）。毛利氏はCIY保守のインフォメックス担当で、連絡はTeams側の可能性（Slackには本人チャンネルなし）。

## 2026-07-16 保守判定・工数算出の打ち返し（川崎さんフィードバックで確定した追加ルール）
- **背景**: サイト修正・改修依頼を受けたとき、川崎さんが毎回「①素材そろってる？→②保守内か/保守外なら何時間か→③影響範囲」を判断している。これをAIの打ち返し草案に組み込む。手順は [[maintenance-judgment]]（`vault/70_references/maintenance-judgment.md`）に集約。基準の原文は `vault/_cache/maintenance-guide/index.md`。
- **無償枠の箇所数はスタックで違う（確定）**:
  - **通常制作＝月3箇所まで／sumabi(Elementor)＝月2箇所まで**（"タグ改変を伴わない軽微対応"が条件、繰越なし）。
  - guideには通常の月3箇所は明記があるが**sumabiの「月2箇所」は川崎さんフィードバックで確定した追加ルール**。guide本体には未記載なので judgment 側で保持する。
- **Elementor構築案件＝sumabi案件で、ローカルレポジトリが無いことがある（確定）**:
  - その場合は**段階3(影響範囲)をコードで自動確認できない**→「実機・WordPress管理画面での人手確認が必要」と明記する。推測でコードの影響範囲を断定しない。
- **技術スタックは案件ナレッジ(context.md)に組み込む（方針・川崎さん指摘）**:
  - context.md に「### 技術スタック」を設け、構築方式（通常制作/自社CMS/フレームワーク/**Elementor(WordPress)**）とローカルレポジトリ有無を記録。これが段階0(前提識別)の入口＝保守枠(3 or 2)とコード確認可否を即決できる。
- **迷ったら川崎に回す（ルール化）**: 工数表に該当なし/線引きが割れる/金額確定提示/影響範囲大 は独断で確定せず、判定サマリに「要・川崎判断」と明示（guide受付フローC/Dの"迷ったら川崎に相談"を再現）。
- **金額の自動確定はしない**: 1h≈6,000円は概算目安に留め、**具体的な金額提示は要・川崎確認**扱い。

## 2026-07-16 vault frontmatter の手動編集でYAMLを壊し重複カードを生成（自戒）
- **やらかし**: item .md の `thread_last_id` を python の `re.sub(r'^thread_last_id:.*$', ...)` で1行だけ置換した。だが値が複数行の折り畳みスカラー（`thread_last_id: >-` ＋次行にインデントされた値）だったため、2行目が孤児になりYAMLが不正化。`listItems`(gray-matter)がそのカードを読めず、ingestが既存カードに照合できずに**重複カードを新規生成**した。
- **教訓**: vault の frontmatter を sed/regex で書き換えるときは、値が複数行スカラー（`>-` `|` 等）になり得ることを常に想定する。1行だけ置換しない。
- **正しいやり方**: ①gray-matter等パーサ経由で読んで書き戻す、②どうしても正規表現なら折り畳みスカラー継続行まで含めて置換（例: `thread_last_id:\s*(>-|\|-|>|\|)?\s*\n(\s+\S.*\n)?` を1行へ）、③編集後は必ず `listItems` で件数・照合を確認する。
- **関連の設計耐性**: ingest 側は「同一thread_keyの既存カードがあれば新規作成しない」設計だが、パース不能で"存在しない扱い"になると防げない。frontmatterを壊さないことが第一。
