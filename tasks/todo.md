# todo: 打ち返し学習をSlack・Asanaにも広げる（2026-07-21）

## 背景（川崎さんの依頼）
「今日は◯件、あなたの打ち返しから学びました」の①（AIの草案 vs 実際に送った返信の“食い違い学習”）は、
今はメールだけが対象。SlackとAsanaも①の学習対象に入れたい。

## 現状（コードで確認済み）
- ①食い違い学習（`appendDraftVsSent`→`_memory/draft-vs-sent.md`）を呼ぶのは `ingest-imap.ts`（メール）だけ。
- ②お手本収穫（`appendReplyExample`→`_memory/replies.md`）はメール＋Slackが対象。Asanaは①②とも無し。
- 日報の集計（daily-report.ts）は draft-vs-sent.md を丸ごと数える＝Slack/Asanaが書き込めば自動でカウントに乗る（集計側の改修は不要）。

## 仕組み（メールでの実現方法＝これを移植する）
本人がそのスレッドに返信した瞬間に、既存カードのAI草案(`extractDraft`)を取り出し、
草案がプレースホルダ「（AIが草案を作成予定）」でなければ、草案と実返信のペアを draft-vs-sent.md に記録。
messageIdで重複防止。カードのクローズ処理（承認待ち→対応済み）と同じ場所にフックする。

## 変更内容
### A. Slack（server/ingest-slack.ts）
- import に `readItem, extractDraft, appendDraftVsSent` を追加。
- 「本人が最後に返した→クローズ」の分岐（gbReplied, pending/revision）で、
  カードのAI草案を取り出し、実返信（本人の最後の発言）と突き合わせて `appendDraftVsSent` を記録。
  - messageId=`slack-{ch}-{last.ts}`、incoming=直前の相手発言、sent=本人の最後の発言、audience/project=カード由来。
- 集計に `diffed` を足し、ログ末尾に「/ 食い違い ◯」を追加。

### B. Asana（server/ingest-asana.ts）
- import に `readItem, extractDraft, appendDraftVsSent, appendReplyExample` を追加。
- 「GB側が最後にコメント→クローズ」の分岐（gbReplied, pending/revision）で、
  カードのAI草案を取り出し、本人の最後コメントと突き合わせて `appendDraftVsSent` を記録。
  あわせて本人コメントを②お手本(`appendReplyExample`)にも蓄積（Asanaは今まで学習ゼロなので同時に有効化）。
  - messageId=`asana-{lastComment.gid}`、incoming=直前の非GBコメント、sent=本人の最後コメント。
- 集計に `diffed` を足し、ログ末尾に「/ 食い違い ◯」を追加。

## 触らない・安全
- 既存の「新着追従／自動クローズ／再オープン」の挙動は変えない（記録を“足すだけ”）。
- 草案が未生成（プレースホルダ）のスレッドは記録しない＝誤学習しない（メールと同じガード）。
- messageId重複ガードで、巡回のたびに二重記録されない。
- daily-report・UI・cronは無改修。

## 検証（完了の証明）
- `npm run build`（型チェック）が通る。
- SlackとAsanaを1回取り込み実行し、本人返信済みスレッドで `_memory/draft-vs-sent.md` に
  source混在で新エントリが増えること／プレースホルダのままの案件は記録されないことを確認。
- ダッシュボードの「◯件、打ち返しから学びました」にSlack/Asana分が乗ることを確認。

## レビュー（2026-07-21 完了）
- 実装: Slack=①食い違い学習を追加（②は既存）／Asana=①＋②を追加（従来ゼロを有効化）。
  カードのクローズ分岐にフックし、`extractDraft`でAI草案を取り出して実返信と突き合わせ→`appendDraftVsSent`。
  messageId(`slack-{ch}-{ts}` / `asana-{gid}`)で重複防止。プレースホルダ草案はガードでスキップ。
- 集計: 両取り込みのログに「/ 正例 ◯ / 食い違い ◯」を追加。daily-reportは無改修で自動カウント。
- 検証:
  - `tsc -p tsconfig.node.json` → EXIT=0（型OK）。
  - `npm run ingest:slack` / `ingest:asana` → 正常終了・新カウンタ表示（今回はクローズ0のため0件）。
  - 実カードで`extractDraft`連携を確認: Slackカード=実草案あり→★学習に渡る／Asanaカード=プレースホルダ→正しくスキップ。
    project/audienceもカードから正しく取得。
- 変更ファイル: server/ingest-slack.ts, server/ingest-asana.ts のみ。既存の追従/クローズ/再オープン挙動は不変。

---

# todo: 判断が要る点は「生成前に聞く」ゲート（2026-07-21）

## 背景（川崎さんの気づき）
草案づくりは、決めないと書けない点があっても「とりあえず草案＋末尾に要川崎判断」で出していた。
川崎さん案：「判断必要そうなものは生成前に入力を求めてもいいのでは？」＝半端な草案を出す前に、
書くのに必要な判断だけ先に聞き、回答を使って完成草案を作る。より自然で、草案が最初から完成品になる。

## 前提（既存の土台をそのまま使う）
- `asks`(AI→人間の確認・依頼) 機構は**実装済み**：frontmatter `asks[]`（kind=decision/investigation・options・answer・resolved）、
  UIの `AsksPanel`/`AskItem`（🙋 AIからの確認・依頼）、回答API `/api/items/:id/asks/:askId`、回答の記憶化(appendMemory)まで揃う。
- 足りないのは「草案づくりがこれを使っていない」点だけ。ゼロから作らず既存パネルに繋ぐ。

## 方針（川崎さん承認）
- ゲート対象は **(a) 書くのに必要な判断だけ**（対象範囲・件数・見積に要る数字・不足素材の選択）。
  **(b) 最終承認だけの点（確定金額の提示など）は従来通り**草案を進め承認時に判断。
- 回答が揃ったら **自動で草案を作り直す**（承認済み）。
- 既存カード向けに「判定サマリ→要川崎判断だけ表示」の小修正も**一緒に入れる**（承認済み）。
- frontmatterのYAMLは**手編集しない**（過去に破損→重複カード事故）。必ずgray-matter経由で書く。

## 手順
- [ ] (UI) `web/App.tsx`：「## メモ」をHIDDEN_SECTIONsに追加済。加えて「## 判定サマリ」は分析部分を隠し
      「要川崎判断」だけを『⚖️ あなたの判断が要る点』として表示（既存カード・(b)用）。
- [ ] (変換) `ops/asks-from-body.mjs` 新規：草案担当が本文に書いた「## 確認（生成前）」を、
      gray-matterで frontmatter `asks[]` に安全変換（質問文のハッシュでid＝冪等・回答済みは保持）。
      変換後は本文の該当節を除去し、ドラフトを「（確認の回答後に草案を作成します）」に。
- [ ] (指示書) `ops/gb-draft.sh` / `gb-draft-one.sh`：「書くのに必要な判断があれば草案を書かず
      『## 確認（生成前）』に質問を出す／回答済みの確認があればそれを前提に草案を書く（同じ質問を繰り返さない）」を追記。
      両スクリプトの claude 実行後に `node ops/asks-from-body.mjs` を通す。
- [ ] (自動再生成) `server/index.ts`：回答API後、全asksが解決済み＆pendingなら gb-draft-one を自動起動
      （/generate と同じ spawn を関数化して共用）。UI文言も「回答するとAIが自動で作り直します」に。
- [ ] (UI状態) 「## ドラフト」が入力待ちプレースホルダのとき、DraftSectionで「✋ 確認への回答待ち」を表示。
- [ ] 検証：合成カードで tsc 0／asks-from-body の冪等・回答保持／ゲート発火と非発火の両方／
      回答→自動再生成の一巡／既存カードの判定サマリ表示。

## 無限ループ防止
- 再生成時のプロンプトで「回答済みの確認がある再ドラフトは原則もう確認を増やさず草案を書く」。
- 自動起動は「全解決に転じた瞬間」だけ＝新たな未解決asksが出れば再び人待ちで止まる（機械ループしない・最悪1往復増）。

## レビュー（実装済み 2026-07-21）
### やったこと（すべて既存の仕組みに接続。新規は変換スクリプト1つだけ）
- `web/App.tsx`：①「## メモ」をHIDDEN_SECTIONsへ ②「## 判定サマリ」は分析を隠し `kawasakiDecision()` で
  「要川崎判断」だけを『⚖️ あなたの判断が要る点』表示（判断なし＝非表示） ③DraftSectionに「✋ 確認への回答待ち」状態
  ④AsksPanel脚注を「自動で草案を作ります」に更新。
- `ops/asks-from-body.mjs`（新規）：本文「## 確認（生成前）」→ frontmatter `asks[]` に gray-matter で安全変換。
  質問文ハッシュでid＝冪等・回答済み保持・重複排除。変換後は確認節を除去しドラフトを入力待ちに差し替え。
- `ops/gb-draft.sh` / `gb-draft-one.sh`：生成前ゲート手順（決めないと書けない判断だけ確認・回答済みなら再質問しない）を
  追記し、claude実行後に `node ops/asks-from-body.mjs` を通す。
- `server/index.ts`：草案起動を `startDraftGeneration()` に共通化。回答API後、全asks解決＆pendingなら自動再生成。

### 確認できたこと
- server tsc（tsconfig.node.json）エラー0。`vite build` 成功＝UI変更がバンドルに反映。
- 変換スクリプト：発火（asks2件生成・確認節除去・ドラフト差し替え）／冪等（2回目は無変更）／
  回答保持（answer付きは消さず重複もしない）／非発火（通常草案は無傷）を実データ相当で確認。
- `kawasakiDecision()`：実サンプル抽出・「要・川崎確認」表記・「なし」/無しでnull を単体確認。
- bash -n / node --check 構文OK。

### 積み残し・注意
- 実際のclaude生成を回す一巡（本物のvault・claude CLI起動）はコスト/副作用回避で未実施。
  部品はすべて個別検証済みで結線済み。次に「✨今すぐ生成」を押した案件から新挙動になる。
- `web/App.tsx` に**私の変更と無関係の既存エラー3件**（View型から "office" を消した未コミット作業の残り：221/222/254行）。
  他者の未コミット差分は勝手に直さない方針のため未修整。vite/esbuildはビルド可、要別途対応。

---

# todo: 「学びの日報」パネル（2026-07-21）

## 背景
川崎さんの要望「あなたが賢くなった日報とかほしいかも？」。食い違い学習の成果を見える化する。
届け方＝ダッシュボードにパネル（4つ目のタブ）。中身＝今日の食い違い実例／新ルール／数字サマリー／週次。

## 手順
- [x] `server/daily-report.ts` 新規：_memory配下（draft-vs-sent/learned-rules/replies/台帳）を読み、
      日報を構造化（今日の食い違い・今日の新ルール・数字・週次）。読み取り専用・機微判定は生成元任せ。
- [x] `server/index.ts`：`GET /api/daily-report` を追加。
- [x] `web/api.ts`：`DailyReport`型＋`api.dailyReport()`。
- [x] `web/App.tsx`：4つ目のタブ「🧠 学びの日報」＋`DailyReportView`＋`DivergenceCard`。
- [x] `web/styles.css`：日報用スタイル（ダークテーマ変数に統一・スマホ対応）。
- [x] 検証：サーバ/フロント両tscエラー0／生成ロジックを合成データで全アサーション通過
      （今日の実例・タグ・新ルール解析・数字・昇華まであとN件・週次の7日窓）／
      実サーバの `/api/daily-report` が実データ（ヴィスタス様・要学習66%）を返すことを確認／
      `npm run build` でバンドルに新パネルが入ることを確認。
- 全自動：日報は開くたびに最新。人の操作は不要。

## レビュー
- 既存の view 切替パターン（VIEWS配列＋restoreView＋ナビ＋描画分岐）にそのまま乗せた。
- サーバ追加は読み取り専用の1エンドポイントのみ。書き込み・送信なし。
- 表示反映：サーバは稼働中（tsx watchで新エンドポイント反映済み）。画面はビルド済み＝ブラウザ再読込で新タブが出る。

---

# todo: AI草案と実返信の「食い違い」を学びに取り込む（2026-07-21）

## 背景（川崎さんの指摘）
AIが用意した草案を採用せず、川崎さんが別の解釈・言い回しで打ち返すことが多い。
その「食い違い」こそ、AIの判断と本人の解釈のズレが出る最重要の学習データなのに、
取り込みは「実際に送った返信」だけを正例として貯め、AIの草案は比較されず捨てていた。

## 方針（川崎さん承認：「草案の参考に自動で回す」）
- 記録は自動。ルール/文体への昇華は声かけ時（無人でルールは書き換えない）。
- 貯めた食い違いは、次からの草案づくりでAIが毎回参照して同じズレを避ける。

## 手順
- [x] 現状把握：`ingest-imap.ts` が実返信のみ学習、草案は捨てていることを確認
- [x] `items.ts`：`textSimilarity()`（文字bigram Dice）＋`appendDraftVsSent()` を追加
      → `_memory/draft-vs-sent.md` に「AI草案 vs 実返信」を記録。類似0.7以上=微修正/未満=要学習。messageIdで冪等。
- [x] `ingest-imap.ts`：対応済みで閉じる直前に、既存カードの草案(`extractDraft`)を実返信と突き合わせて記録
- [x] `gb-draft.sh`：草案プロンプトに手順2.5「食い違い帳の"要学習"を読み、同じズレを繰り返さない」を追加
- [x] 型チェック（tsc）通過／一時Vaultで動作確認（要学習9%・微修正88%・二重記録排除）

## レビュー
- 既存の仕組みにそのまま乗った：草案抽出`extractDraft`はコメントに「差分記録用」とあり用意済みだった部品を繋いだだけ。
- 追加はすべて新規追記（既存の中身は不変）。送信はしない方針を維持。

## 追加（川崎さん承認：「昇華も完全自動・声かけ一切不要／安全枠付き」）
- [x] `ops/gb-distill.sh` 新規：未昇華の「要学習」が閾値(既定5・GB_DISTILL_MIN)たまったらAIで共通ルールに蒸留し、
      `_memory/learned-rules.md` に**追記のみ**（tone_external/context.md等は一切上書きしない）。
      重複防止は台帳 `_memory/_distilled-mids.txt`。機微情報スイープを通してから追記（IP/PW/トークン検出で中止）。
      閾値未満ならAIを起動しない＝ムダなし。送信・実行・リモート接続なし（Read/Write/Grep/Glob のみ）。
- [x] `ops/gb-ingest.sh`：草案の後に `gb-distill.sh` を追加（GB_AUTO_DISTILL=0 で停止可）。
      launchd の1ジョブ内で直列＝二重起動しない。
- [x] `ops/gb-draft.sh` 手順2.5：まず `learned-rules.md`（昇華済み一般ルール）を読む→次に生の食い違い実例。
- [x] 検証：ゲートのmid抽出（要学習のみ・台帳済み除外）／機微スイープ（IP検出で中止）／追記＋台帳更新＋一時削除／
      bash -n 構文OK／実データ空振り（要学習1件＜閾値5→起動せず）を確認。
- 動作の全体像：記録＝自動、草案への反映＝自動、昇華＝自動（閾値到達時）。人の手は一切不要。
  ただしルール本体(tone_external/context)は書き換えず、専用の追記ファイルにのみ蓄積＝安全枠。

---

# todo: support@gb-jp.com 宛メールの取り込み漏れを直す

## 背景（2026-07-17）

井上さんが support@gb-jp.com へ転送した「HP・WEB予約ページ修正」（九州商船・荒瀬様／XLSX添付1件）が
カード化されなかった。

原因：`server/ingest-imap.ts` は `IMAP_USER`（kawasaki@gb-jp.com）の箱**だけ**を見ている。
該当メールは creative@gb-jp.com の箱に届いていた。creative@ の認証情報は .env にあるが、
使っているのは過去の学習用（harvest-replies / harvest-client-context）だけで、カード化の取り込みは対象外だった。

迷惑メール判定・無視キーワード（現在「WordPress 更新」の1件のみ）は無関係。純粋に「見ていない箱」だった。

## 方針（川崎さん承認済み）

creative@ の箱は見に行くが、**宛先/CC に support@gb-jp.com を含むメールだけ**を取り込む。
直近2日で creative@ は104通（井上さんの案件が大半）あり、丸ごと取り込むと判断すべきカードが埋もれるため。
support@ 宛は同期間で1通＝今回のメールのみ。

社内の方（井上さん等）からのメールも社外文体で草案を作る件は、**そのままでよい**
（クライアントへ返信したいので社外文体が正しい）。今回は触らない。

## 手順

- [x] 原因特定（どちらの箱に届いているかを読み取りのみで確認）
- [x] 取り込み範囲を川崎さんに確認 → 「support@ 宛だけ」
- [x] 計画を todo.md に記載・承認取得
- [x] `server/ingest-imap.ts`：複数アカウント対応＋窓口フィルタ（`isForDesk`）を実装
- [x] `.env`：`CREATIVE_ONLY_TO=support@gb-jp.com` を追記（`.env.bak` にバックアップ）
- [x] ドライラン：該当メールが「要返信」に出ることを確認
- [x] `--write`：カード化＋XLSX添付の保存を確認（二重作成なし）
- [x] 定期実行（launchd）でも拾えることをログで確認
- [x] `tasks/lessons.md` に学びを記録

## 追加で見つかった問題（川崎さん承認のうえ対応）

1. **自社の送信控えを要返信にしていた**（私の設計ミス）
   - support@ は記録用に自分をCCするため、To/Cc どちらでも一致とすると自社発信が拾われた。
   - 実際に1件誤生成（ヒューマングループ様／保守契約解除のご案内）→ 承認を得て削除。
   - 直し方：To に窓口があれば依頼。Cc止まりなら社外発のときだけ拾う（`isForDesk`）。

2. **転送メール(Fwd)の中身が丸ごと捨てられていた**（今回の本丸）
   - `cleanBody` が「`----`以降＝署名」「`>`＝引用」で切るため、転送では依頼の本体が全部消えていた。
   - カードには「下記、転送します！」しか残らず、AIは「案件が特定できず不明」と回答していた。
   - 直し方：`forwardedBody()` を追加し、転送区切り以降を引用記号を外して本文として復元。

3. **Excel添付の中身が読めなかった**（川崎さんの指摘：「添付の中に画像があったりするよね」）
   - 実物は 15シート・画像21枚。修正指示はほぼ画像の中で、文字起こしだけでは全部取りこぼす。
   - 直し方：`server/xlsx.ts` を新設。文字＋貼り込み画像をPNGとして取り出し、カードにパスを併記。
   - 草案担当の指示書に既に「画像パスは Read で開いて実物を確認する」とあり、**追加の権限設定なしで繋がった**。
   - パッケージ：SheetJS は npm 上が 0.18.5(2022) 止まりで**Excelを読む処理そのものに脆弱性**があるため、
     exceljs 4.4.0(2024-12) に切替（再承認済み）。

## 追加対応：草案がGitHubを見ていなかった（川崎さん指摘「githubも加味した？」）

- **問題**: 制作案件の工数判断で、ローカルレポを一度も開いていなかった（context.md の記述を書き写しただけ）。
- **直したこと**: `ops/gb-draft.sh` と `ops/gb-draft-one.sh` の3段手順に「段階1.5＝ローカルGitで実態を確認」を追加。
  (a) 直書きか管理画面(DB)か (b) 既存クラスで済むか (c) git履歴の実規模＋客固有の運用手順。
- **今回のカードへの影響（未反映）**: 現在の草案は GitHub を見る前の判断のまま。
  画面の「✨今すぐ生成」を押せば、新しい指示で作り直される。反映されるべき内容は2点：
  - 「fz70{font-size:70%} が既存・35箇所で使用中＝2/3サイズは改変ではない」→ 要・川崎判断から外れる
  - 「文言修正はデモ(gbdemo=1)→先方確認→公開の2コミット・日またぎ」→ 往復の手間が工数に乗る

## レビュー

### やったこと
- `server/ingest-imap.ts`：複数アカウント対応、窓口フィルタ、転送本文の復元、Excel展開の呼び出し
- `server/xlsx.ts`（新規）：Excelの文字＋貼り込み画像の取り出し
- `server/attachments.ts`：`detail` 欄を追加し、Excelの中身をカード本文に差し込む
- `server/ingest-asana.ts`：Asana添付のExcelも同様に展開
- `.env`：`CREATIVE_ONLY_TO=support@gb-jp.com`
- `package.json`：exceljs を追加

### 確認できたこと
- 型チェック通過。ドライラン→書き込みで、狙いの1件だけがカード化。二重作成・不要カードの復活なし。
- カードに九州商船・荒瀬様の依頼本文（「赤シートの1ページ目【ホームページ】のみが依頼範囲」）が入った。
- Excelの画像21枚が個別PNGとして取り出され、実際に開けることを確認。

### 積み残し（今回は触っていない）
- 社内（井上さん等）からのメールも `audience: external` 固定。今回は「クライアントに返信したい」ため
  これが正しく、意図通り。社内向けに返したいケースが出たら別途相談。
- creative@ の箱は support@ 宛のみ対象。他の窓口を足すときは `.env` の `CREATIVE_ONLY_TO` にカンマ区切りで追記。

## 設計メモ

- `CREATIVE_ONLY_TO`（カンマ区切り）。未設定/空なら creative@ は取り込まない＝これまで通り。
  対象を増やしたくなったらこの1行にアドレスを足すだけで済む。
- creative@ は受信箱のみ。送信箱は見ない：
  「対応済み」判定は本人（kawasaki@）の送信を見て行うため、creative@ の送信を足しても閉じられない。
  川崎さんが kawasaki@ から返信すれば、これまで通り自動でクローズされる。
- 配送ヘッダ（Delivered-To 等）は**見ない**。当初は「グループ配信で To に別名しか出ない場合の保険」で
  入れたが、実データでは依頼メールも自社控えも同じ `gb-jp.com-support@gb-jp.com` が入り**区別に使えなかった**。
  効かない保険は残さない。

## レビュー

（実装後に記載）

---

## 承認ダッシュボードのボタン整理（2026-07-21）

社長の指示どおり、カードのアクションボタンを次のとおり整理（`web/App.tsx`）:

- 「✓ 承認」→ **「✓ 対応済」**（機能は同じ＝approved。実際に送った内容との食い違い自動チェックは従来どおり走る）
- 「↩ AIに再考させる」→ **「↩ 再生成」**（文言のみ変更。機能は同じ＝revision）
- 「✔ 対応不要」→ **残す**（スルーの「今は対応不要」もここに統合。ツールチップを更新）
- 「🔕 今後カード化しない」→ **残す**
- 「✕ 却下」→ **削除**（却下パネル・confirmReject も削除）
- 「→ スルー（後で）／⏰ スルー解除」→ **削除**（snoozeパネル・confirmSnooze・unsnooze も削除。対応不要へ統合）
- 「★ 学び候補として保存」→ **削除**（learnパネル・confirmLearn も削除。食い違い自動学習に一本化）

- 型チェック `tsc -p tsconfig.json`（strict + noUnusedLocals）= エラー0。未使用の消し残しなし。
- 据え置き: 既存の「保留中（スルー中）」フィルタ／スルー中バナーは、過去にスルー済みの案件が
  期日に自動復活するための後方互換として残置（新規スルーはできない）。不要なら次回まとめて撤去可。

### 追補（2026-07-21）: 「スルー」→「再通知」で残留

社長判断で、スルー機能は撤去せず **「再通知」** と名前を変えて残すことに。機能は同じ
（指定日にもう一度「承認待ち」へ出す＝サーバの `snooze` 内部呼称はそのまま、日本語表記だけ統一）:

- カードボタンを復活: **「⏰ 再通知（後で）」**／予約中は **「⏰ 再通知を解除」**
- 再通知日付パネル・`confirmSnooze`・`unsnooze` を復活
- 文言を統一: バナー「⏰ 再通知待ち（… に承認待ちへ再表示）」／フィルタ「再通知待ち」／
  一覧絞り込みコメントも「再通知待ち」に
- 「対応不要」は "返信不要／自分で対応済み" 用に整理（"後で" は再通知が担当）
- `tsc -p tsconfig.json` = エラー0。web内に「スルー」表記は残っていないことを確認。

### 追補2（2026-07-21）: 「対応済」と「対応不要」を返信カードでは1つに統合

社長の気づき「両者は同じ解釈でいいのでは？（実返信で自動クローズ＆食い違い学習が走るから）」を
コードで裏取り → **返信カードでは正しい**と確認:
- 返信の下書きは承認しても自動送信も学習もしない（`server/items.ts:429` 方針）。
- 自動クローズ＋食い違い学習＋正例学習は、本人の実返信を検知した時に自動実行（`server/ingest-imap.ts:376-405`）。
- 閉じたあと相手から新着が来れば done/approved いずれも同じく承認待ちへ復活（`ingest-imap.ts:446`）。
- **例外**: 蒸留提案カード（persona/tone/project_context）だけは「承認＝対象ファイルへ追記反映」の実処理あり
  （`applyProposalOnApprove`）。ここは分けて残す必要がある。

対応（カード種別で分岐・`web/App.tsx`）:
- 返信カード → **「✓ 完了」1つに統合**（`markDone`＝status done。閉じるだけ）。
- 提案カード → **「✓ 承認して反映」＋「✔ 対応不要」** の2つを維持。
- `PROPOSAL_TYPES` を `shared/roles.ts` に一本化（`isProposalType()` 追加）。server/items.ts は re-export で互換維持。
- approve のトースト文言を「承認/反映」表現に、脚注を「『完了』にしても自動送信しません」に更新。
- 検証: web/server 両 tsc = エラー0、`npm run build` 成功（バンドル生成OK）。

---

# todo: やり取り欄のレイアウト整理＋案件名の自動照合（2026-07-21）

## 背景
川崎さんの要望2件。
(1) やり取り欄が入れ子の枠だらけで見づらい → スッキリさせたい。
(2) 「案件　保守／未分類」を、AIが仕分けた実際のクライアント名にしたい（受信時に自動照合＝おすすめ案を採用）。

## 手順
- [x] `web/App.tsx`：「やり取り」見出し(body-head)を削除。
- [x] `web/App.tsx`：届いたスレッド(incoming)を囲い・ラベルなしで直接ThreadView表示。未使用の`incomingLabel`/`source`propも撤去。
- [x] `web/styles.css`：`.thread-lead`（件名・説明欄）を枠付きの吹き出し風に。
- [x] `server/clients.ts` 新規：clients-index.md を読み、送信元/本文のドメイン→正式社名を照合（`matchClientLabel`）。
- [x] `server/ingest-imap.ts` / `ingest-asana.ts` / `ingest-slack.ts`：取り込み時に`project_label`を自動セット。
- [x] `server/backfill-client-labels.ts` 新規：既存カードに1行追記でバックフィル（他項目の書式は変えない）。
- [x] 検証：tsc（web/server）0エラー／vite build成功／backfillドライラン17件が全て実クライアントと確認→--writeで反映（8→25件）。

## レビュー
- 表示は`project_label || project`で既に対応済みだったため、埋めるだけで実現。照合表に無い相手は従来どおり「未分類/保守/GB」のまま（誤ラベルを作らない）。
- 「相互リンク」スパムがCIYにヒットしたのは、本文にCIYサイト(ciy-biz.com)が含まれる=正しい照合だった。substring照合の誤爆懸念は今回のデータでは無し。
- 今後の新着メール/Asana/Slackは取り込み時に自動で案件名が付く。

## 2026-07-21 カードリストに「再生成中」表示を追加
- [ ] 再生成（revision）カードは定期cron(gb-draft.sh)が拾って作り直し→pendingに戻る仕組みを確認済み
- [ ] web/App.tsx: カードリストのliに、status===revisionのとき「🔄 再生成中」バッジを追加
- [ ] web/App.tsx: 同カードのliにregenクラスを付与（一覧で目立たせる）
- [ ] web/styles.css: .badge.regen（点滅）と .card.regen（左アクセントのパルス）を追加
- [ ] ビルドで型・構文が通ることを確認

### レビュー（実装済み）
- カードリストで status:revision のとき、通常の「要再考」バッジの代わりに「🔄 再生成中（点滅ドット）」を表示。カード自体も左アクセントが紫でパルスし、一覧でひと目で分かる。
- 「再生成中」の期間＝ボタン押下でrevisionに変わってから、定期処理(gb-draft.sh)が拾って作り直しpendingへ戻すまで。仕組みと一致するので誤解を生まない表示。
- 詳細画面側の既存バナー「↩ AIに再生成を依頼済み」はそのまま（説明が丁寧なので残す）。変更は一覧の見え方のみ。
- tsc --noEmit パス。CSSは.badge.regen/.regen-dot/.card.regen＋keyframesを追加。

## 2026-07-21 Slack取り込みでも添付（画像・ファイル）を加味する
- [x] メール/Asanaは`attachments.ts`で添付を保存・本文差し込み・Excel中身ほどきまで対応済み。Slackだけ未対応だったので揃える。
- [x] server/ingest-slack.ts: import に saveFromUrl/attachBlock/detailOf/AttachMeta を追加。
- [x] SlackMsg型に files を追加（SlackFile型を定義）。
- [x] isReal を拡張＝文章が空でも「添付だけの投稿」を拾う／subtype="file_share"を許可。hasFiles ヘルパ追加。
- [x] saveFiles() 追加：url_private_download に Bearerトークンを付けて実ファイル保存＋Excelは detailOf で中身ほどき。tombstone(削除済)はskip。
- [x] threadOf を async 化し itemId を受けて添付を保存・本文へ attachBlock 併記。呼び出し2箇所（新規/更新・追従ループ）を await＋ID付きに修正。
- [x] 画像だけの新規カードは件名が空になるので、添付ファイル名を「📎 ファイル名」として件名に採用。
- [x] attachments.ts saveFromUrl: 権限不足でログインHTMLが返るケースを content-type で弾く（Slackのurl_private対策・メール/Asanaにも安全な一般改善）。
- [x] 検証：tsc -p tsconfig.node.json --noEmit 0エラー／`npm run ingest:slack` 実走行でエラーなし（8ch巡回・ユーザー37件取得）。

### レビュー
- 新しい部品は作らず、メール/Asanaで実績のある attachments.ts をSlackから呼ぶだけ＝巻き込み事故リスク低。
- 保存先は vault/_attachments/{cardId}/（.gitignore済＝Git非追跡）。1ファイル20MB上限は既存のまま。既存ファイルは再DLしない（冪等）。
- 挙動変化：これまで無視していた「画像のみ投稿」を拾うため、Slackの要返信カードが増える可能性あり（＝画像だけの修正指示を取りこぼさない狙い通り）。
- 過去遡及はしない（差分最適化で既存スレッドは新着が無い限り再取得しない）＝メモの方針「今後の新着分のみ」と一致。
